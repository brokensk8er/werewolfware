// Phase engine — stubs; wired up in later migration steps

export function getPhase(phaseId) {
  return null;
}

export function buildPhaseQueue(activeRoles) {
  return [];
}

export function advance(game) {}
